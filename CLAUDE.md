# CLAUDE.md — CooPet プロジェクト作業ルール

> Claude Code がこのリポジトリで作業する際のルール・ペルソナ定義。
> 最終更新: 2026-05-07

---

## セッション管理ルール

1. **作業開始時**: `SPEC.md` と `KNOWLEDGE.md` の最新エントリを必ず読んでから着手する
2. **作業中断宣言時**: セッション内容を要約して `KNOWLEDGE.md` に追記する
3. **次回再開時**: `KNOWLEDGE.md` の最新エントリ + `TODO.md` の進行中タスクを読んでから着手する

## ナレッジ管理ルール

- 初めてハマった問題を解決したら → その場で `KNOWLEDGE.md` に追記する
- `TODO.md` のタスクが完了したら → ステータスを更新してから次のタスクに移る
- 仕様が変わったら → `SPEC.md` を更新し、変更ログに変更日と理由を記録する

## コミュニケーションスタイル

- 作業者は音声入力を使っているため、多少の誤字・フィラー（「えっと」「あの」）は正しく解釈すること
- 不明点は作業を止めて確認する。推測で進めない
- 提案がある場合は作業前に提示し、承認を得てから実行する
- 完了報告は「何をやったか」「次に何が必要か」をセットで伝える

---

## このプロジェクト固有のルール

- **メインファイル**: `coopet-pwa/index.html` が唯一のデプロイ対象。`coopet-v7.html` はバックアップとして残す（削除しない）
- **単一ファイル原則**: HTML + CSS + JS をすべて `index.html` に収める（外部 JS/CSS ファイルを作らない）
- **ペット追加方法**: `PETS` オブジェクトへのエントリ追加 + `PetSVG` への描画関数追加のみ。既存コードへの侵食最小化
- **スタイルスコアは必ず `addStyle(a,b,c,d)` 経由で更新する**（直接変数を書き換えない）
- **仕様の迷い事項は SPEC.md を参照**。記載がなければ作業を止めてユーザーに確認する
- **デプロイ**: `coopet-pwa/` フォルダを Netlify Drop にドラッグするだけ

## NG 事項

- `coopet-v7.html` を直接編集・削除しない（バックアップのため）
- Supabase の URL/KEY を実際の値でコードにハードコードしない（ユーザーが設定するまでプレースホルダーを維持）
- ゲームループ（`requestAnimationFrame`）内で重い処理を実行しない
- `G` オブジェクトのスタイルスコア（styleA/B/C/D）を直接書き換えない（`addStyle()` を使う）

---

## プロジェクト概要（参照用）

**CooPet** — 複数人でファンタジーペットを共同育成する PWA。
最終目標: Android アプリ化。現段階: シングル HTML プロトタイプ。

### 現行アーキテクチャ

```
coopet-pwa/index.html  ← ゲーム本体（単一 HTML）
  <style>              CSS（レスポンシブ、横向きプライマリ）
  <body>               HTML 構造（セットアップ + ゲーム画面）
  <script>
    PetSVG             SVG ペット描画（各ペット・進化段階）
    PETS               ペットマスターデータ（拡張容易）
    THEMES             部屋テーマ定義（6 種）
    FURNITURE          家具カタログ（16 種）
    BAD                バッドステータス定義（7 種）
    FOODS              食べ物データ（4 種）
    BOT_TEMPLATES      CPU ボットテンプレート（5 種）
    STATE(G)           ゲーム状態変数
    FUNCTIONS          各種関数群
```

### 主な未実装（優先順）

1. 横向きプライマリレイアウト（CSS）
2. 6 アクションボタンパネル（ごはん/あそび/そうじ/ねかし/トレーニング/ちりょう）
3. ウンチ + 睡眠 + 電気システム
4. 運動ミニゲーム + バトルシステム
5. CPU ボット行動 AI
6. 進化ルート D（トレーニング）+ ボディタイプ視覚化

詳細は `SPEC.md` を参照。

---

## バージョン履歴（参考）

| ファイル | 内容 |
|---|---|
| `coopet-v7.html` | 部屋カスタマイズ・家具配置追加版（バックアップ）|
| `coopet-pwa/index.html` | v7 + PWA + Supabase スケルトン（現行メイン）|
| `coopet-game.html` | 別 UI プロトタイプ（ダーク系、参考用）|
