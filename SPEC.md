# SPEC.md — CooPet 仕様定義

> 最終更新: 2026-05-07
> 変更時は末尾の変更ログに記録する

---

## プロジェクト概要

**CooPet** は複数人でファンタジーペットを共同育成するスマートフォン向け Web アプリ（PWA）。
最終目標は Android アプリ化。現段階は **シングル HTML プロトタイプ**。

---

## ファイル構成

```
coopet-pwa/
├── index.html        ← ゲーム本体（現行 1246 行、今後大幅拡張）
├── manifest.json     ← PWA 定義
├── sw.js             ← Service Worker（オフライン対応）
└── icons/            ← PWA アイコン 8 種

coopet-v7.html        ← v7 シングルプレイ原本（バックアップ）
coopet-game.html      ← 別 UI プロトタイプ（ダーク系、参考用）
supabase-schema.sql   ← DB スキーマ（接続待ち）
```

---

## 技術スタック

| 項目 | 内容 |
|---|---|
| 実装形式 | 単一 HTML（HTML + CSS + Vanilla JS） |
| 外部依存 | Google Fonts のみ（CDN）|
| フレームワーク | なし |
| バックエンド | なし（全フロントエンド、将来 Supabase 予定）|
| PWA | manifest.json + sw.js |

---

## 画面構成

```
セットアップ画面（3 ステップ）
  Step1: ペット選択（卵カード）
  Step2: 部屋テーマ選択（6 種）
  Step3: ペット名入力

ゲーム画面（横向きプライマリ）
  ┌────────────────────┬──────────────────────┐  ← Landscape
  │  ルームエリア       │  アクションパネル      │
  │  （左 58%）         │  6 ボタン 2×3 Grid    │
  │  ペット + 家具      ├──────────────────────┤
  │  HUD + 電気ボタン   │  ボトムタブ           │
  │  ウンチ + 暗闇OL   │  📊 ✨ 👥 💬         │
  └────────────────────┴──────────────────────┘

  縦向き時: ルーム(上) → アクションパネル → タブ（縦積み）
```

---

## ペットシステム

### ペット種類（現状 3 種、拡張容易な設計）

| キー | 名前 | 属性 |
|---|---|---|
| `dragon` | ドラゴン系 | 火属性 |
| `unicorn` | ユニコーン系 | 魔法属性 |
| `slime` | スライム系 | 変身属性 |

> **新ペット追加方法**: `PETS` オブジェクトにエントリを追加し、`PetSVG` に描画関数を追加するだけ。選択画面に自動反映される。

### 進化段階（4 段階）

```
[たまご期] → [Lv2] → [Lv3] → [最終形態 × 4 ルート]
```

### 進化ルート（4 種）※ 現行 3 種から拡張予定

| ルート | 条件 | 最終形態の特徴 |
|---|---|---|
| A: 食いしん坊 | styleA が支配的 | 太ってぽっちゃり体型 |
| B: バランス | 全スコアが均等 | 理想的なバランス体型 |
| C: あそび | styleC が支配的 | 丸くてかわいい体型 |
| D: トレーニング | styleD が支配的 | ムキムキ筋肉体型 |

**ボディタイプ視覚化**: SVG ラッパーに CSS transform を適用
- chubby: `scaleX(1.35) scaleY(1.15)`
- muscular: `scaleX(1.22) scaleY(0.88)`
- cute: `scale(1.12)`
- balanced: `scale(1.0)`

### スタイルスコア加算量

| 操作 | styleA(食) | styleB(均) | styleC(遊) | styleD(鍛) |
|---|---|---|---|---|
| ごはんドロップ | +8 | +2 | +1 | 0 |
| なでる | 0 | +2 | +6 | 0 |
| くるくる | 0 | +3 | +8 | 0 |
| つつく | 0 | +1 | +4 | 0 |
| 運動ミニゲーム | 0 | +2 | 0 | +5 |
| バトル勝利 | 0 | +1 | 0 | +6 |
| そうじ | 0 | +1 | 0 | +2 |

---

## ステータス定義

### メインステータス（G オブジェクト）

| 変数 | 名前 | 範囲 | 説明 |
|---|---|---|---|
| `hunger` | おなか | 0–100 | 減少: 自然減(1.2/10sec), 少ない: 飢え |
| `happy` | きもち | 0–100 | 減少: 自然減(0.6/10sec) |
| `energy` | げんき | 0–100 | 減少: 自然減(0.3/10sec), 運動/バトルで大幅減 |
| `xp` | 経験値 | 0–上限 | 各ステージで閾値に達すると進化 |
| `physical` | からだ | 0–100 | 運動で上昇、自然減少 |
| `clean` | 清潔度 | 0–100 | ウンチ放置で減少、そうじで回復 |
| `stageIdx` | 進化段階 | 0–3 | 3 = 最終形態 |
| `pts` | ポイント | 0– | プレイヤーの貢献ポイント |
| `styleA/B/C/D` | 育て方スコア | 0– | 進化ルート判定に使用 |

### 睡眠ステート

| 変数 | 値 | 意味 |
|---|---|---|
| `sleepState` | `'awake'` | 通常起動中 |
| `sleepState` | `'drowsy'` | ねむそう（ねかしアクション有効） |
| `sleepState` | `'sleeping'` | 就寝中（食事/遊び/トレーニング無効） |
| `lightOn` | boolean | 部屋の電気状態 |
| `lightPenaltyCount` | 数値 | 就寝中に電気オンの継続カウント |

### ウンチ状態

| 変数 | 型 | 説明 |
|---|---|---|
| `poops` | Array | `{id, el, spawnTime}` の配列 |
| `lastPoopTime` | timestamp | 最後のウンチ発生時刻 |

---

## バッドステータス（7 種）

| ID | 名前 | 発症条件 | 自動回復条件 | 効果 |
|---|---|---|---|---|
| `fat` | 肥満 | hunger > 92 | hunger < 55 | — |
| `digest` | 消化不良 | 30秒以内に3回以上給餌 | 給餌が減る | げんき毎tick-2 |
| `tired` | だるい | 20秒以内に15回以上タッチ or 子守唄4回/min | タッチが減る | XP取得50%減 |
| `nervous` | 神経質 | 20秒以内に25回以上タッチ | タッチが減る | タッチ反応が過敏 |
| `sleepy` | 睡眠不足 | げんきが22以下60秒継続 | げんき22超え | げんき上限-15% |
| `lonely` | さみしい | 最後の操作から120秒 | 操作が行われた時 | きもち毎tick-3 |
| `injured` | **けが** | バトル中に15%確率で発生 | ちりょうアクション | 全ステータス毎tick追加-2 |

---

## アクションボタン（6 種）

| ボタン | 役割 | 動作 |
|---|---|---|
| 🍖 **ごはん** | ごはん係 | フードピッカーを表示 → タップで給餌 |
| 🎮 **あそび** | あそび係 | なでる/くるくる/つつく を選択 |
| 🧹 **そうじ** | そうじ係 | ウンチを全て掃除（アニメーション付き） |
| 🌙 **ねかし** | ねかし係 | drowsy時: 就寝開始 / sleeping時: 起こす / awake時: 子守唄 |
| 🏋️ **トレーニング** | トレーナー係 | 運動ミニゲーム or バトルを選択 |
| 💊 **ちりょう** | いしゃ係 | けが状態なら治療 / 正常ならヘルスチェック表示 |

---

## ミニゲーム仕様

### 運動ミニゲーム
- 8秒間 タップ連打
- 結果: `physical += floor(タップ数/3)`, `xp += 15`, `hunger -= 10`, `energy -= 12`
- styleD += 5

### バトル
- モンスター HP: 5
- ATTACKボタンを5回タップで撃破
- 各攻撃: `energy -= 5`, `hunger -= 3`
- 勝利: `xp += 25`, `pts += 20`
- 各攻撃に15%で「けが」バッドステータス発生

### モンスターリスト（拡張予定）
| 名前 | icon | HP | XP |
|---|---|---|---|
| スライムもどき | 👾 | 5 | 25 |
| コウモリ | 🦇 | 8 | 35 |
| ゴーストナイト | 👻 | 10 | 50 |

---

## 睡眠サイクル

```
awake → (2〜4分ごと 60%確率) → drowsy
drowsy → (ねかしアクション) → sleeping
drowsy → (3分放置) → sleeping（自動、ペナルティなし）
sleeping → (90秒後 自動) → awake
sleeping → (ねかしアクション) → awake（ペナルティあり: 全ステータス小幅減）
```

**電気ペナルティ**: `lightOn = true` かつ `sleeping` の状態が続くと `lightPenaltyCount` が増加。
起床時の睡眠品質スコアに影響（不眠ペナルティ）。

---

## ウンチサイクル

- 90秒〜3分ごとにランダム発生（起動中のみ、就寝中は発生しない）
- 最大 4 個まで同時存在
- 2分以上放置: `clean -= 10/10sec`
- そうじアクション: 全ウンチ除去、`clean += count*5`

---

## CPU ボットシステム（ダミーマルチプレイ）

ソロプレイユーザー向けに AI ボットを参加させてマルチプレイを疑似再現。

### ボットテンプレート（5 種）

| ID | 名前 | 役割 | 行動間隔 | 主な行動 |
|---|---|---|---|---|
| `misaki` | みさき 🦊 | あそび係 | 45秒 | なでる・きもち回復 |
| `takeshi` | たけし 🐻 | ねかし係 | 60秒 | 子守唄・電気管理 |
| `yui` | ゆい 🐼 | そうじ係 | 50秒 | ウンチ掃除 |
| `kota` | こうた 🐯 | トレーナー係 | 70秒 | 運動補助 |
| `hana` | はな 🐰 | ごはん係 | 40秒 | 給餌 |

### ボット動作
- 各ボットは専門アクションを一定間隔で実行（プレイヤーより小さな効果）
- チャットに状況に応じた発言を投稿
- メンバータブに `💻 CPU` バッジ付きで表示
- ゲーム開始時に3体のダミーメンバーをデフォルト表示（実際のボット行動はオプション）

---

## メンバー役割（6 種）

| 役割 | ラベル | 担当アクション |
|---|---|---|
| ごはん係 | 🍖 | 給餌 |
| あそび係 | 🎮 | なでる・くるくる・つつく |
| そうじ係 | 🧹 | ウンチ掃除 |
| ねかし係 | 🌙 | 就寝管理・電気管理 |
| トレーナー係 | 🏋️ | 運動・バトル |
| いしゃ係 | 💊 | けが治療・ヘルスチェック |

---

## 部屋カスタマイズ

### テーマ（6 種）
`night` / `forest` / `ocean` / `sunset` / `snow` / `sakura`

### 家具（16 種）
🌲🌸🛋️🪔🪨🍄📦💧🪧🧺🕯️💎⭐🔔📚⚽

### 電気システム
- 💡 ボタンで `lightOn` トグル
- 消灯時: 暗闇オーバーレイ表示（z-index 8、ペットは z-index 10 で視認可）

---

## 進化ルート判定ロジック

```javascript
function getRoute() {
  const tot = G.styleA + G.styleB + G.styleC + G.styleD;
  if (!tot) return 'B';
  const ra = G.styleA/tot, rb = G.styleB/tot, rc = G.styleC/tot, rd = G.styleD/tot;
  const mx = Math.max(ra, rb, rc, rd);
  if (mx - Math.min(ra,rb,rc,rd) < 0.2) return 'B'; // バランス
  if (mx === rd) return 'D'; // トレーニング
  if (mx === ra) return 'A'; // 食いしん坊
  if (mx === rc) return 'C'; // あそび
  return 'B';
}
```

---

## マルチプレイ（将来実装）

現在はプレースホルダーのみ。将来的に Supabase Realtime で接続。
- `supabase-schema.sql` は完成済み（実行待ち）
- URL パラメータ `?room=xxx` でのルーム参加フローのコードは記述済み
- Supabase URL/KEY がプレースホルダーのため非接続

---

## 実装状況（2026-05-07 現在）

### ✅ 実装済み
- セットアップ画面（3ステップ）
- ペット SVG アニメーション（全種）
- ルーム + テーマ + 家具配置
- 食べ物ドラッグ給餌
- なでる・つつく・くるくるジェスチャー
- バッドステータス（fat/digest/tired/nervous/sleepy/lonely）
- 進化システム（3ルート → 4ルートに拡張予定）
- 進化カットシーン
- ダミーメンバー表示・チャット（ランダム返信）
- Supabase 統合コード（未接続）
- PWA 対応（install banner, sw.js）

### 🔄 実装予定（次フェーズ）

| 機能 | 優先度 | 備考 |
|---|---|---|
| 横向きプライマリレイアウト | ★★★ | CSS media query |
| 6 アクションボタンパネル | ★★★ | ごはん/あそび/そうじ/ねかし/トレーニング/ちりょう |
| ウンチシステム | ★★★ | タイマー発生・そうじアクション |
| 睡眠サイクル + 電気ボタン | ★★★ | drowsy/sleeping state |
| 運動ミニゲーム | ★★★ | 8秒タップ連打 |
| バトルシステム | ★★★ | 5hit + けが発生 |
| けが/ちりょうシステム | ★★★ | 新バッドステータス |
| CPU ボット（行動AI） | ★★☆ | 5テンプレート |
| 進化ルート D（トレーニング） | ★★☆ | ムキムキ体型 |
| ボディタイプ視覚化 | ★★☆ | CSS transform |
| フードピッカー UI | ★★☆ | ドラッグ廃止→タップに |
| 縦向きレイアウト | ★★☆ | portrait fallback |
| Supabase 接続 | ★☆☆ | バックエンド化フェーズ |

---

## 対象外（スコープアウト）

- ユーザー認証（現時点）
- リアルタイム DB 接続（プロトタイプフェーズ）
- BGM・効果音
- アルバム機能
- スケジュール当番管理
- ローカルストレージ保存（現時点）

---

## 仕様変更（2026-05-08 追加）

### 部屋テーマ（具体的なデザインに変更）

| ID | 名前 | 種別 | 説明 |
|---|---|---|---|
| `kids_room` | こどもへや | 室内 | カラフルな壁・玩具が並ぶ子供部屋 |
| `cozy_room` | きのうちのへや | 室内 | 木目調フローリング・暖かい照明 |
| `magic_cave` | まほうのどうくつ | 屋外（洞窟） | 光る鉱石・青紫の神秘的空間 |
| `forest_field` | もりのひろば | 屋外 | 木漏れ日・草地・遠くに木々 |
| `beach` | うみべ | 屋外 | 砂浜・水平線・夕暮れの空 |
| `snow_peak` | ゆきやま | 屋外 | 雪景色・星空・凍った地面 |

### チャット表示ルール
- チャット欄（メッセージリスト＋入力フォーム）は **チャットタブ選択時のみ** 表示
- 他タブ切り替え時は非表示（DOM から `display:none`）

### ペットタップ誤認防止
- タップ後 **2 秒間** はペットへの入力を無視するクールダウンを追加
- `mouseover` / `mouseenter` 等のホバーイベントによる反応を除去
- `touchstart` と `click` の二重発火を防ぐため `touchstart.preventDefault()` を使用

### ウンチ発生頻度
- 現行: 90 秒ごと → 変更: **15〜60 分ランダム**（900,000〜3,600,000 ms）
- 就寝中・部屋が暗い場合はウンチしない

### バトル（タイミング式）
- **タイミングバー**: 横に往復するカーソルがあり、緑ゾーンでタップするとダメージ
  - 緑ゾーン(中央): 1 ダメージ ＋ `Great!` 表示
  - 黄ゾーン(左右): 0 ダメージ（スカ） ＋ `Miss!` 表示
  - Miss 時: **反撃**として 20% の確率でけが発生
- **負け条件**: ミス 3 回でバトル失敗 → けがバッドステータス確定
- 勝利: モンスター HP を 0 にする（緑ヒット × 5 回）

### バトル・トレーニングアニメーション
- バトル中: ペット SVG に `battle-anim` CSS クラス付与（左右シェイク）
- トレーニング中: ペット SVG に `exercise-anim` CSS クラス付与（上下バウンス）

### 食べ物（2 種に変更）

| ID | 名前 | 空腹回復 | その他効果 | クールダウン |
|---|---|---|---|---|
| `meat` | 🥩 お肉 | +28 | バトル中ダメージ +1（battleBoost フラグ） | 45秒 |
| `fruit` | 🍎 くだもの | +12 | きもち+8・げんき+5 | 30秒 |

### トースト通知の位置
- 現行: 画面上部 → 変更: **画面中央**（`top:50%; transform:translateY(-50%)`）

### ねかしメカニクス（ガラガラなでる）
- **ねかし** ボタン押下 → 「ガラガラ🪇モード」に移行
- ガラガラモード中はペットエリアに🪇アイコンが表示
- ペット上でスワイプ（なでる操作）を行うと寝かしつけが進行
- 3 ストローク完了で就寝開始
- 20 秒以内に完了しないとモードキャンセル

### 家具・模様替えシステム

#### 模様替えモード
- 部屋右上の 🛋️ ボタンで **模様替えモード** をトグル
- 通常モード: 家具はタップ・ドラッグ不可（固定表示のみ）
- 模様替えモード: グリッドオーバーレイ表示、家具の配置・移動・削除が可能
- 模様替えモード中は HUD に「✏️ 模様替え中」バナーを表示

#### グリッド配置
- 部屋を **4×3 グリッド**（横4列×縦3行）に分割
- 家具はグリッドセルにスナップして配置（無段階配置廃止）
- 各セルに置ける家具は 1 個まで
- 家具アイコンサイズ: `font-size: 32px`（現行 24px から拡大）

---

## 変更ログ

| 変更日 | 変更内容 | 理由 |
|--------|----------|------|
| 2026-05-07 | 初版作成 | セッション記録 |
| 2026-05-07 | 進化ルートを3→4種に拡張（D: トレーニング追加） | ユーザー要件：ムキムキ体型の進化先 |
| 2026-05-07 | バッドステータスに「けが」追加 | バトルシステム導入に伴う |
| 2026-05-07 | アクションボタン6種定義（ごはん/あそび/そうじ/ねかし/トレーニング/ちりょう） | ユーザー要件 |
| 2026-05-07 | 役割6種に更新（旧4種から拡張） | アクションボタンに対応 |
| 2026-05-07 | 横向きプライマリレイアウトに変更 | ユーザー要件：ペット画面が小さすぎる |
| 2026-05-08 | 部屋テーマを具体的な室内/屋外デザインに変更 | ユーザー要件 |
| 2026-05-08 | チャット欄をチャットタブのみに制限 | ユーザー要件 |
| 2026-05-08 | タップ2秒クールダウン・ホバー誤認防止 | ユーザー要件：誤タップ問題 |
| 2026-05-08 | ウンチ頻度を15〜60分ランダムに変更 | ユーザー要件 |
| 2026-05-08 | バトルをタイミング式に変更・負け条件追加 | ユーザー要件 |
| 2026-05-08 | 食べ物を肉・くだもの2種に変更 | ユーザー要件 |
| 2026-05-08 | トースト通知を画面中央に移動 | ユーザー要件 |
| 2026-05-08 | ねかしをガラガラなで方式に変更 | ユーザー要件 |
| 2026-05-08 | 模様替えモード追加・グリッド配置・家具拡大 | ユーザー要件 |
